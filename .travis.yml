language: go
go:
- 1.15.x

before_script:
  - sudo apt-get install zip
  - sudo apt -y install ninja-build
addons:
  apt:
    update: true

script:
  - ./pre-commit
  - cd build
  - go build ./cmd/bood
  - bood
  - cd ../examples
  - go build ../build/cmd/bood
  - bood